version: '3'

services:
    db:
        image: postgres
        environment:
            POSTGRES_DB: "db"
            POSTGRES_PASSWORD: "Gusy7070"

    web:
        build: .
        volumes:
            - .:/usr/src/app
        ports:
            - "8000:8000"
